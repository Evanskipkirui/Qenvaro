# Dependencies — never push this (100MB+, reinstalled with npm install)
node_modules/

# Build output — generated files, not source code
dist/
build/

# Environment files — may contain secrets
.env
.env.local
.env.production

# Editor/OS files
.DS_Store
Thumbs.db
.vscode/settings.json

# TypeScript cache
*.tsbuildinfo
